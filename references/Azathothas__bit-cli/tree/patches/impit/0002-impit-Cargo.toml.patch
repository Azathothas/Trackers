# impit: impit/Cargo.toml
#
# Against https://github.com/apify/impit at 4fd6c3167c55d9d059a3e5872846e0b5c0a31e3b.
# Generated by scripts/vendor-diff.ps1 from vendor/impit. Do not edit:
# the vendored tree is the truth and this is derived from it. What the
# change is for is patches/UPSTREAM.md.
diff --git a/impit/Cargo.toml b/impit/Cargo.toml
index e286cb0..b255e9e 100644
--- a/impit/Cargo.toml
+++ b/impit/Cargo.toml
@@ -2,23 +2,27 @@
 name = "impit"
 version = "0.1.0"
 edition = "2021"
+license = "Apache-2.0"
 
 # See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
 
 [dependencies]
 bytes = "1"
-encoding = "0.2.33"
 futures-core = "0.3"
-hickory-proto = "0.26.1"
-hickory-resolver = "0.26.1"
 log = "0.4.22"
-mime = "0.3.17"
-reqwest = { version="0.13.1", features = ["json", "gzip", "brotli", "zstd", "deflate", "http3", "cookies", "stream", "socks"] }
+# No `http3`: reqwest gates it behind `--cfg reqwest_unstable`, which would
+# have to be added to all three .cargo/config.toml target blocks and all
+# three RUSTFLAGS blocks in ci.yml, where a workflow RUSTFLAGS replaces the
+# config rather than adding to it. That is the shape of T-146. See
+# patches/UPSTREAM.md.
+reqwest = { version="0.13.1", features = ["json", "gzip", "brotli", "zstd", "deflate", "cookies", "stream", "socks"] }
 rustls = { version="0.23.36", features=["impit"] }
-lol_html = "2.7.2"
 thiserror = "2.0.12"
-tokio = { version="1.40.0", features = ["full"] }
 url = "2.5.2"
+# `h2` for the pseudo-header order type and `http` for the request round
+# trip that carries it. Both are already in the graph under `reqwest`.
+h2 = "0.4.19"
+http = "1"
 rustls-platform-verifier = "0.6"
 webpki-root-certs = "1.0.5"
 hyper-util = "0.1.18"
@@ -28,3 +32,4 @@ uuid = { version = "1", features = ["v4"] }
 
 [dev-dependencies]
 futures-util = "0.3"
+tokio = { version = "1.40.0", features = ["full"] }
