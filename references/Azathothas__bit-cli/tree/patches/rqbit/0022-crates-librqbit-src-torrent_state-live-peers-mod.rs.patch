# rqbit: crates/librqbit/src/torrent_state/live/peers/mod.rs
#
# Against https://github.com/ikatson/rqbit at a499d2f243d124e144aef137afe7cb304a6e3f36.
# Generated by scripts/vendor-diff.ps1 from vendor/rqbit. Do not edit:
# the vendored tree is the truth and this is derived from it. What the
# change is for is patches/UPSTREAM.md.
diff --git a/crates/librqbit/src/torrent_state/live/peers/mod.rs b/crates/librqbit/src/torrent_state/live/peers/mod.rs
index d80b6e9..9ccfb97 100644
--- a/crates/librqbit/src/torrent_state/live/peers/mod.rs
+++ b/crates/librqbit/src/torrent_state/live/peers/mod.rs
@@ -16,6 +16,19 @@ use super::peer::{LivePeerState, Peer, PeerRx, PeerState, PeerTx};
 
 pub mod stats;
 
+/// Retained peer rows per torrent, past which adding one reclaims one.
+///
+/// Nothing here used to give a row back. A peer that hands over cleanly ends in
+/// `NotNeeded` and stays: twenty-four handshake-and-close connections from
+/// loopback left twenty-four rows, `live` and `dead` both zero, and a minute of
+/// silence returned none of the memory. A row is **2,891 bytes**, measured over
+/// 2,000 of them at r squared 0.94, which is most of a long seeder's memory
+/// slope. See `TODO/memory.md`, T-040.
+///
+/// 1,024 is well above any real swarm's live peer count, so a working torrent
+/// never reaches it and this only ever reclaims history.
+pub(crate) const MAX_PEER_RECORDS: usize = 1024;
+
 pub(crate) struct PeerStates {
     pub session_stats: Arc<AggregatePeerStatsAtomic>,
 
@@ -23,6 +36,8 @@ pub(crate) struct PeerStates {
     pub live_outgoing_peers: RwLock<HashSet<PeerHandle>>,
     pub stats: AggregatePeerStatsAtomic,
     pub states: DashMap<PeerHandle, Peer>,
+    /// Zero disables reclamation, which is what this was before.
+    pub max_records: usize,
 }
 
 impl Drop for PeerStates {
@@ -38,8 +53,53 @@ impl PeerStates {
         self.stats.snapshot()
     }
 
+    /// Reclaim retained rows until the map is back inside `max_records`.
+    ///
+    /// Only `NotNeeded` and `Dead` are taken. A `Live` or `Connecting` row has
+    /// a task behind it, and a `Queued` one is a peer we still mean to dial;
+    /// those are the rows a bound must never touch, which is why this counts
+    /// what it can take rather than evicting by age.
+    ///
+    /// Called before an insert rather than after, and never while an `Entry` on
+    /// this map is held: `DashMap` locks per shard and taking a second guard on
+    /// the same shard deadlocks.
+    pub fn reclaim_records(&self) {
+        if self.max_records == 0 {
+            return;
+        }
+        let len = self.states.len();
+        if len < self.max_records {
+            return;
+        }
+        // One below the bound, because the caller is about to insert: reclaim
+        // to `max_records` here and the map settles at one over it forever.
+        let over = len - self.max_records + 1;
+        let mut reclaimable = Vec::with_capacity(over);
+        for entry in self.states.iter() {
+            if reclaimable.len() >= over {
+                break;
+            }
+            if matches!(
+                entry.value().get_state(),
+                PeerState::NotNeeded | PeerState::Dead
+            ) {
+                reclaimable.push(*entry.key());
+            }
+        }
+        // The iterator above holds a shard guard, so nothing is removed until
+        // it has been dropped.
+        for addr in reclaimable {
+            if let Some((_, peer)) = self.states.remove_if(&addr, |_, p| {
+                matches!(p.get_state(), PeerState::NotNeeded | PeerState::Dead)
+            }) {
+                peer.destroy(self);
+            }
+        }
+    }
+
     pub fn add_if_not_seen(&self, addr: SocketAddr) -> Option<PeerHandle> {
         use dashmap::mapref::entry::Entry;
+        self.reclaim_records();
         match self.states.entry(addr) {
             Entry::Occupied(_) => None,
             Entry::Vacant(vac) => {
