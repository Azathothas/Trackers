# h2: Cargo.toml
#
# Against https://github.com/hyperium/h2 at d57d1b852fec9dda6d42d3454502006d52104da8.
# Generated by scripts/vendor-diff.ps1 from vendor/h2. Do not edit:
# the vendored tree is the truth and this is derived from it. What the
# change is for is patches/UPSTREAM.md.
diff --git a/Cargo.toml b/Cargo.toml
index 53acb79..23aca8e 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -31,12 +31,16 @@ unstable = []
 
 [workspace]
 members = [
-    "tests/h2-fuzz",
     "tests/h2-tests",
     "tests/h2-support",
     "util/genfixture",
     "util/genhuff",
 ]
+# tests/h2-fuzz needs honggfuzz, which refuses to build on Windows, and this
+# repository is developed on Windows. Kept in the tree and out of the
+# workspace, which is what upstream rustls does with its own fuzz crate.
+# See patches/UPSTREAM.md.
+exclude = ["tests/h2-fuzz"]
 
 [dependencies]
 atomic-waker = "1.0.0"
